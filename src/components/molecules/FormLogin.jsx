import { useNavigate } from "react-router-dom";

function FormLogin() {
  const navigate = useNavigate();
  return (
    <div>
      <div>
          <h2>BIENVENIDO</h2>
      </div>
    </div>
  );
}

export default FormLogin;
